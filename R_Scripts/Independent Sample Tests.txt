# Independent Sample Tests

independent_t <- read_excel("C:/Users/acer/OneDrive/Desktop/AA/Advance_Analytics/independent_t.xlsx")
attach(independent_t)

# To find the structure of data
str(independent_t)

# Summary of data
summary(independent_t)

# Factor of data
independent_t$city <- as.factor(independent_t$city)
str(independent_t)

shapiro.test(Perception)

# Call the car package
leveneTest(Perception~city, data = independent_t)
t.test(Perception ~ city , data = independent_t, var.equal = TRUE) # if var h
t.test(Perception ~ city , data = independent_t)
